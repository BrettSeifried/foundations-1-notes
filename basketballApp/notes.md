Home Team       visitor
score           score

btn -3 pt       btn 3ptn
btn FreeT       btn FreeT
fieldGoal       fieldGoal

# HTML elements
* 6 buttons - static
* 2 headers - static
* 2 spans for points - dynamic

# state
* home score ~ 0
* visitor score ~ 0

# EVENTS
* button clicks

--------

## Testing using Cypress: Unit & integration testing
* Cypress simulates opening a web browser and checking to see if everything works.
* node package manager ("npm")
    * allows third party packages to add functionality
* npm -i every time cloning from GitHub
    * Install = -i
* init -y to install JSON package
* npm i "dependenciy name
* gitignore should always have files in there ignored by git.
* don't push nodes_modules folder
    * gitignore node_modules = "node_modules"
